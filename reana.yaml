workflow:
  type: serial
  specification:
    steps:
      - name       : test 
        kerberos   : true
        environment: 'docker.io/acampove/rx_run3:v4.1'
        kubernetes_memory_limit: '256Mi'
        commands:
        - |
            pip install --target /tmp/.rx_local --no-deps -r requirements.txt

            export PYTHONPATH=/tmp/.rx_local
            python -c "import dmu"
