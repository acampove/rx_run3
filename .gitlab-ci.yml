# Taken from:
# https://gitlab.cern.ch/gitlabci-examples/build_docker_image/-/blob/master/.gitlab-ci.yml?ref_type=heads#L54
stages:
  - test
  - build
  - toys 
build-image:
  stage: build
  image: registry.cern.ch/quay.io/buildah/stable
  before_script:
    - sed -i 's/^driver = .*/driver = "vfs"/' /etc/containers/storage.conf
    - echo "$CI_REGISTRY_PASSWORD" | buildah login -u "$CI_REGISTRY_USER" --password-stdin $CI_REGISTRY
  script:
    - buildah bud  -t $CI_REGISTRY_IMAGE:c76a965c5 .
    - buildah push    $CI_REGISTRY_IMAGE:c76a965c5
  only:
    - master
toys:
  stage: toys 
  image: $CI_REGISTRY_IMAGE:c76a965c5
  script:
    - reana-client create -w toys 
    - reana-client start  -w toys --follow
