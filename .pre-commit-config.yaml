repos:
  - repo: local
    hooks:
      - id      : replace-sha
        name    : Replace :latest with SHA from Snakefile
        entry   : hooks/replace_with_sha.sh
        language: script
        stages  : [pre-commit]  # Runs on git commit
        types   : [yaml]  # Optional: Only run if YAML files are staged; adjust as needed
