saving:
    plt_dir : tests/plotting/no_bounds

plots:
    x : 
        binning : [0, 0, 40]
        yscale  : 'linear' 
        labels  : ['x', 'Entries']
    y : 
        binning : [0, 0, 40]
        yscale  : 'linear' 
        labels  : ['y', 'Entries']
