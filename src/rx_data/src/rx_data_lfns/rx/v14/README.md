# Description

These LFNs correspond to `v2` for the configs of the `rk` and `rkst` projects and contain:

- Data with refitting
- MC from `sim10f` with resonant, semileptonic, partially reconstructed rare RKst samples
