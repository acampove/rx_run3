training :
    nfold    : 10
    features : [w, x, y, z]
    hyper    :
      loss              : log_loss
      n_estimators      : 100
      learning_rate     : 0.1
      min_samples_split : 2 
saving:
    path : 'tests/train_mva/model.pkl'
plotting:
    val_dir : 'tests/train_mva'
    features:
        saving:
            plt_dir : 'tests/train_mva/features'
        plots:
          w : 
            binning : [-4, 4, 100]
            yscale  : 'linear' 
            labels  : ['w', '']
          x : 
            binning : [-4, 4, 100]
            yscale  : 'linear' 
            labels  : ['x', '']
          y : 
            binning : [-4, 4, 100]
            yscale  : 'linear' 
            labels  : ['y', '']
          z : 
            binning : [-4, 4, 100]
            yscale  : 'linear' 
            labels  : ['z', '']
   
