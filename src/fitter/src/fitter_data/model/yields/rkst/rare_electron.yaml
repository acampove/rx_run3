# This config file defines the yields and the relationships between them
# For the fit of the rare electron channel 
yields: &yields
  val : 0
  min : 0
  max : 100000

yld_signal:
  <<      : *yields
yld_ccbar:
  <<      : *yields
yld_Bu_Kpipiee_eq_DPC_LSFLAT:
  <<      : *yields
yld_combinatorial:
  <<      : *yields
yld_jpsi_leakage:
  <<      : *yields
yld_psi2_leakage: 
  <<      : *yields
